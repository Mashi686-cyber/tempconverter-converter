version: "3.8"
services:

  mongodb:
    image: mongo:8.0
    container_name: tempconverter-mongodb

  tempconverter:
    build: .
    image: tempconverter:latest
    container_name: tempconverter-api

    ports:
      - "8081:8081"
    depends_on:
      - mongodb

volumes:
  mongo-data: